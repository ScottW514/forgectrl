{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "panel: dev server",
      "detail": "Serve the panel from src/ui.c with live reload; API from the machine in .env, or the mock",
      "type": "shell",
      "command": "python3",
      "args": ["tools/devserver.py"],
      "isBackground": true,
      "problemMatcher": [],
      "presentation": { "reveal": "always", "panel": "dedicated", "group": "panel" },
      "runOptions": { "runOn": "folderOpen" }
    },
    {
      "label": "panel: dev server (mock)",
      "detail": "Same, but always answer the API from the built-in mock backend",
      "type": "shell",
      "command": "python3",
      "args": ["tools/devserver.py", "--mock"],
      "isBackground": true,
      "problemMatcher": [],
      "presentation": { "reveal": "always", "panel": "dedicated", "group": "panel" }
    },
    {
      "label": "build (host)",
      "detail": "cmake --build build (native forgectrl + the host unit tests)",
      "type": "shell",
      "command": "cmake -B build -DCMAKE_EXPORT_COMPILE_COMMANDS=ON && cmake --build build -j$(nproc)",
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": ["$gcc"]
    },
    {
      "label": "tests (host)",
      "detail": "The CI unit tests",
      "type": "shell",
      "command": "for t in status_idle_test status_grbl_test curverec_fit_test status_sys_test sanitize_test debayer_test lid_gate_test camhealth_test cool_gate_test cool_flow_test airflow_test accel_test coolfmt_test auth_peer_test mp4mux_test; do ./build/$t || exit 1; done",
      "dependsOn": ["build (host)"],
      "group": { "kind": "test", "isDefault": true },
      "problemMatcher": []
    }
  ]
}
